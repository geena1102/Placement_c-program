#include<stdio.h>
int main()
{
    int num,rem,sum=0,large=0;
    scanf("%d",&num);
    while(num!=0)
    {
    rem=num%10;
    if(rem>large)
    {
        large=rem;
    }
    num=num/10;
    }
    printf("%d",large);
}

