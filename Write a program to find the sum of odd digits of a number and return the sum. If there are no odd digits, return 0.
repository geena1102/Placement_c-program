#include<stdio.h>
int main ()
{
    int num,sum=0;
    scanf("%d",&num);
    while(num!=0)
    {
        int d=num%10;
        if(num%2!=0)
        {
             sum=sum+d;
        }
        num=num/10;
    }
  
    
printf("%d",sum);



}

